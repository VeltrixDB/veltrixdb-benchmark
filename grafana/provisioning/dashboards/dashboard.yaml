apiVersion: 1

providers:
  - name: VeltrixDB Benchmarks
    orgId: 1
    folder: VeltrixDB
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
